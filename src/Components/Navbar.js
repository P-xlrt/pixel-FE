export const Navbar = () => {
  const somethingorother = something;

  return (
    <nav>
      {/* Using unorderd list as a placeholder for react router */}
      <ul>
        <li>Create</li>
        <li>View Pixel Art</li>
        <li>Login/Sign-up</li>
      </ul>
    </nav>
  );
};
